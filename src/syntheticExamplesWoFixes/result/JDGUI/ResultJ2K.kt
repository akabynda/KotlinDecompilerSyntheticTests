/*
package syntheticExamplesWoFixes.result.JDGUI

object ResultJ2K {
    fun getResult(value: Int): Any {
        return if ((value > 0)) constructor - impl(value) else constructor - impl(createFailure(Exception("Value must be positive")))
    }

    fun main() {
        val result = getResult(-1)
        var object1 = result
        if (isSuccess - impl(object1)) {
            val i = (object1 as Number).toInt()
            var `$i$a$`: Int
            -onSuccess - `ResultKt$main$1` = 0
            println("Success: $i")
        }
        object1 = object1
        val throwable1: Throwable = exceptionOrNull - impl(object1)
        val it = throwable1
        var `$i$a$`: Int
        -onFailure - `ResultKt$main$2` = 0
        println("Error: " + it.message)
        exceptionOrNull - impl(object1)
    }
}
 */