package syntheticExamples.result.JDGUI

// doesn't work

/*
object ResultJ2K {
    fun getResult(value: Int): Any {
        return if ((value > 0)) constructor - impl(value) else constructor - impl(
            kotlin.ResultJ2K.createFailure(
                Exception("Value must be positive")
            )
        )
    }

    fun main() {
        val result = getResult(-1)
        var object1 = result
        if (isSuccess - impl(object1)) {
            val i = (object1 as Number).toInt()
            var `$i$a$`: Int
            -onSuccess - `ResultJ2K$main$1` = 0
            println("Success: $i")
        }
        object1 = object1
        val throwable1: Throwable = exceptionOrNull - impl(object1)
        val it = throwable1
        var `$i$a$`: Int
        -onFailure - `ResultJ2K$main$2` = 0
        println("Error: " + it.message)
        exceptionOrNull - impl(object1)
    }
}
 */